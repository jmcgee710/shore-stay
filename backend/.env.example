# PostgreSQL connection string for local development
DATABASE_URL="postgresql://username:password@localhost:5432/shore_stay?schema=public"

# JWT secret for auth tokens
JWT_SECRET="replace-with-a-secure-secret"
